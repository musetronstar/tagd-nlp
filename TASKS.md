# TASK

## Goals
Seperate IO from logic
* Modules exposing testable interfaces 
* perl main function does IO: reads/writes, etc

## Instructions

1. See the `TODO` comment block I wrote in `utils/tsv-to-tagl.pl`

2. See the `utils/tests` sub-directory I added. Create unit tests there.

3. Create `Makefile` repository root.
   `make`
   Default Build Target: `tagl/tagd-nlp.tagl`
   `make tests`
   Runs each modules unit tests, etc.


